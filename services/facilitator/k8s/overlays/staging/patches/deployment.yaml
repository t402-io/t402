apiVersion: apps/v1
kind: Deployment
metadata:
  name: facilitator
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: facilitator
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "250m"
