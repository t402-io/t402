name: Go Release

on:
  push:
    tags:
      - "go/v*"
  pull_request:
    paths:
      - "sdks/go/**"
      - ".github/workflows/go_release.yml"
  workflow_dispatch:
    inputs:
      skip_release:
        description: "Skip GitHub release creation (test only)"
        required: false
        default: true
        type: boolean

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: ["1.26"]
    steps:
      - uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go-version }}
          cache-dependency-path: sdks/go/go.sum

      - name: Install dependencies
        working-directory: ./sdks/go
        run: go mod download

      - name: Run tests
        working-directory: ./sdks/go
        run: go test -v -race -coverprofile=coverage.out ./...

      - name: Upload coverage
        if: matrix.go-version == '1.26'
        uses: codecov/codecov-action@v6
        with:
          files: ./sdks/go/coverage.out
          flags: go
          fail_ci_if_error: false

  release:
    needs: test
    if: startsWith(github.ref, 'refs/tags/go/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26"
          cache-dependency-path: sdks/go/go.sum

      - name: Verify module
        working-directory: ./sdks/go
        run: |
          go mod verify
          go mod tidy
          git diff --exit-code go.mod go.sum

      - name: Extract version
        id: version
        run: |
          VERSION=${GITHUB_REF#refs/tags/go/}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Released Go module version: $VERSION"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          name: "Go SDK ${{ steps.version.outputs.version }}"
          body: |
            ## Go SDK ${{ steps.version.outputs.version }}

            ### Installation

            ```bash
            go get github.com/t402-io/t402/sdks/go@${{ steps.version.outputs.version }}
            ```

            ### Changelog

            See [CHANGELOG.md](https://github.com/t402-io/t402/blob/main/sdks/go/CHANGELOG.md) for details.
          draft: false
          prerelease: ${{ contains(steps.version.outputs.version, '-') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
