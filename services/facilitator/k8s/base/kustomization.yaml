apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: t402

labels:
  - pairs:
      app.kubernetes.io/part-of: t402
      app.kubernetes.io/managed-by: kustomize

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - pdb.yaml
  - networkpolicy.yaml
  - ingress.yaml
  - redis.yaml

images:
  - name: ghcr.io/t402-io/facilitator
    newTag: latest
