name: Documentation

on:
  push:
    branches: [main]
    paths:
      - 'sdks/typescript/packages/**/src/**'
      - 'sdks/typescript/typedoc.json'
      - 'services/docs/**'
      - '.github/workflows/api-docs.yml'
  pull_request:
    branches: [main]
    paths:
      - 'sdks/typescript/packages/**/src/**'
      - 'sdks/typescript/typedoc.json'
      - 'services/docs/**'
  workflow_dispatch:

jobs:
  build-and-deploy:
    name: Build and Deploy Docs
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'
          cache-dependency-path: |
            sdks/typescript/pnpm-lock.yaml
            services/docs/pnpm-lock.yaml

      - name: Install TypeScript dependencies
        working-directory: sdks/typescript
        run: pnpm install --frozen-lockfile

      - name: Generate TypeDoc API docs
        working-directory: sdks/typescript
        run: npx typedoc

      - name: Copy API docs to services/docs/public
        run: |
          mkdir -p services/docs/public/api
          cp -r sdks/typescript/api-docs/* services/docs/public/api/

      - name: Install docs dependencies
        working-directory: services/docs
        run: npm install

      - name: Build documentation site
        working-directory: services/docs
        run: npm run build

      - name: Deploy to Cloudflare Pages
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy services/docs/out --project-name=t402-docs

      - name: Deploy Preview
        if: github.event_name == 'pull_request'
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy services/docs/out --project-name=t402-docs --branch=${{ github.head_ref }}
