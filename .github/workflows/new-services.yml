name: New Services CI/CD

on:
  push:
    branches:
      - main
    paths:
      - "services/bazaar/**"
      - "services/status/**"
      - "services/explorer/**"
      - "services/agent-dashboard/**"
      - "services/sandbox/**"
  pull_request:
    paths:
      - "services/bazaar/**"
      - "services/status/**"
      - "services/explorer/**"
      - "services/agent-dashboard/**"
      - "services/sandbox/**"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service:
          - bazaar
          - status
          - explorer
          - agent-dashboard
          - sandbox
    name: test-${{ matrix.service }}
    defaults:
      run:
        working-directory: services/${{ matrix.service }}
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: services/${{ matrix.service }}/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Start server
        run: npm start &
        env:
          BAZAAR_ADMIN_KEY: test-ci-key
          EXPLORER_MODE: seed

      - name: Wait for server
        run: |
          PORT=${{ matrix.service == 'bazaar' && '3402' || matrix.service == 'status' && '3403' || matrix.service == 'explorer' && '3404' || matrix.service == 'agent-dashboard' && '3405' || '3406' }}
          for i in $(seq 1 15); do
            curl -sf http://localhost:${PORT}/health && break
            sleep 1
          done

      - name: Run tests
        run: npm test
        env:
          BAZAAR_ADMIN_KEY: test-ci-key
          BASE_URL: http://localhost:${{ matrix.service == 'bazaar' && '3402' || matrix.service == 'status' && '3403' || matrix.service == 'explorer' && '3404' || matrix.service == 'agent-dashboard' && '3405' || '3406' }}

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to production
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: |
            cd /home/doge/github/t402-main
            git fetch origin main
            git checkout origin/main -- services/
            cd services
            docker compose -f docker-compose.new-services.yml build --parallel
            docker compose -f docker-compose.new-services.yml up -d
