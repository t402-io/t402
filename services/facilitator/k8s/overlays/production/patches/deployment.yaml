apiVersion: apps/v1
kind: Deployment
metadata:
  name: facilitator
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: facilitator
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
