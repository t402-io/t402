[project]
name = "t402-mcp-chatbot"
version = "1.0.0"
description = "Interactive MCP chatbot example for t402 payment tools"
requires-python = ">=3.10"
dependencies = [
    "t402>=1.10.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
